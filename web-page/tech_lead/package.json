{
  "name": "tech-lead-root",
  "version": "1.0.0",
  "description": "Root package to manage tech lead dashboard",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "node startup.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
